Simple arithmetic expression evaluator written in C that uses recursive descent parsing. Calculator supports 4 operations: +, -, *, /, integers
and floating point numbers.

Usage:

make

./calculator

*Enter expression*
